# Agents index (centralizado)

As informações e instruções para agentes automáticos foram consolidadas na pasta `agents/`.

Leia primeiro: [agents/overview.md](agents/overview.md)

Objetivo:
- Evitar duplicação de conteúdo entre o arquivo raiz e `agents/`.
- Manter `agents/` como fonte única para configurações e instruções de agentes.

Notas rápidas:
- Não duplique arquivos explicativos entre a raiz e `agents/` — atualize apenas em `agents/`.
- Ajustes específicos do editor/IDE devem ser colocados em `.vscode/`.

Se precisar, eu posso gerar links diretos ou reorganizar arquivos dentro de `agents/`.
