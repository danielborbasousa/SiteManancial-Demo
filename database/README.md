Pasta `database/` — Dumps e scripts SQL

Conteúdo:
- `Banco Igreja.txt` — versão simples para testes e importação rápida.
- `Banco Igreja Robusto.sql` — versão com schema mais completo e hashes.

Recomendações:
- Inclua apenas scripts pequenos; bancos grandes devem ser compartilhados via storage separado.
- Para exportar: use `mysqldump` e salve com versão clara no nome.
